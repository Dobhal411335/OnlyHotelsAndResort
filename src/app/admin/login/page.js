import { LoginPage } from "@/components/admin/auth/LoginPage";

export const metadata = {
  title: "Admin Login | OnlyHotel CMS",
  description: "Sign in to manage the OnlyHotel website.",
};

export default function AdminLoginPage() {
  return <LoginPage />;
}
